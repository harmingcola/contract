========
Matching
========


* Leaving the body blank in the expected response will cause the ContractClient to ignore any body
that is returned. It will also cause the ContractServer to return an empty body for a request.